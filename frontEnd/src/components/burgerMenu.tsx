import SearchBar from "./searchBar"

export default function BurgerMenu({closeMenu}:{closeMenu: () => void}){
    return(
        <div className="fixed flex flex-col min-h-screen top-26 w-full rounded-lg bg-white p-50 lg:hidden">
            
        </div>
    )
}